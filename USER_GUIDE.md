# DataRobort 用户手册（v1.0）

> DataRobort：自然语言问数 → SQL 自动生成 → 图表分析 → 报告导出，一键部署的企业数据分析助手。

## 1. 快速开始

1. **启动服务**（部署方式见 [DEPLOY.md](DEPLOY.md)）：`docker compose up -d --build`，访问 `http://localhost`。
2. **首次配置三步走**：
   - **模型**：进入「模型配置」，为 Chat 与 Embedding 各添加一个模型（填 base url / API Key / 模型名，保存时自动探测维度），并设为默认。
   - **数据源**：进入「数据源管理」，添加 MySQL 业务库连接，测试连通后点「刷新元数据」抓取表结构（SQL 白名单依赖它）。
   - **智能体**：进入「智能体」，新建 Agent，绑定数据源（可选绑定知识库），发布。
3. **开始问数**：进入「会话」，选一个 Agent，输入自然语言问题，例如：

```
统计orders表总订单数和总销售额
```

## 2. 功能总览（8 个页面）

| 页面 | 功能 |
|---|---|
| 会话 | 核心问数页：自然语言提问、SSE 流式回复、图表、Markdown 报告、多轮追问 |
| 模型配置 | Chat / Embedding 模型管理（OpenAI 兼容），密钥 AES 加密存储 |
| 数据源管理 | 业务库连接管理、连通测试、表元数据采集与刷新 |
| 知识库 | 上传文档（PDF/Word/Markdown/TXT，≤20MB）自动分块向量化 |
| 业务知识 | 维护术语-同义词，增强查询理解（如「大客户」→ VIP） |
| 语义模型 | 表/字段同义词维护（如「流水」→ orders），查询时自动替换 |
| 智能体 | 组合「数据源 + 知识库 + 角色 Prompt」的问答单元，可发布 |
| 报告 | 查看已生成的历史 HTML 分析报告 |

## 3. 问数示例（20+）

### 查询类（直接给数据）
- 统计orders表总订单数和总销售额
- 查询订单金额最高的前5个订单
- 有多少个VIP客户
- 每个城市的客户各有多少
- 订单状态有哪些，每种状态多少单
- 金额超过10000元的订单有多少个
- 库存低于50的产品有哪些
- 产品一共有多少种类目

### 分析类（出图表）
- 分析每月的销售总额趋势
- 按商品类目分析销售额占比
- 分析订单各状态的占比情况
- 对比分析VIP客户与普通客户的订单金额差异
- 分析订单金额的分布区间（小额/中额/大额各多少）
- 分析每月新增客户数量变化
- 分析销售额最高的5个产品类目
- 分析每月的平均客单价变化
- 分析2023年各订单状态的完成情况

### 报告类（生成 HTML 报告）
- 生成一份销售分析报告，包含总销售额、订单数、类目销售额和趋势图表
- 写一份季度销售分析报告并配图
- 生成客户分析报告：客户等级和城市分布
- 写一份库存分析报告，分析库存水平与低库存产品

### 多轮追问（同一会话继续问）
- 第一问：今年总销售额是多少 → 第二问：那订单总数是多少呢 → 第三问：再按城市统计一下销售额

### 闲聊
- 你好，请问你是谁？
- 帮我写一首关于数据库的诗

## 4. 安全须知

- SQL 执行有**四层防护**：AST 校验（仅 SELECT + 表名白名单 + 危险函数黑名单）→ LLM 修复 → 执行前二次校验 + LIMIT 强制 500 + 只读连接 → Druid WallFilter 兜底。
- Python 分析代码在**隔离沙箱**执行：无网络、只读文件系统、全部能力裁剪、512MB 内存 / 1 CPU / 128 进程限制、10 秒强杀、输出 64KB 有界。
- **当前版本无登录认证**，请勿在公网部署（详见 [DEPLOY.md](DEPLOY.md) 已知风险）。

## 5. 多轮会话说明

在会话页发送第一条消息时自动创建会话；后续消息默认追加到当前会话，系统自动携带最近 10 条历史（每条截断 300 字符，总上下文 ≤4000 字符）供模型参考，支持「那订单数呢」「再按城市分组」这类指代追问。

## 6. 常见问题

**Q: 问数一直「服务器繁忙」？**
A: 并发上限（默认 8 条同时执行的图管线）已满，稍后重试；或调大 `datarobort.concurrency.max-graph-threads`。

**Q: 图表一直不出现？**
A: ① 沙箱镜像未构建 → 见 DEPLOY.md 第 5 节；② 问题需要「分析」而非「查询」意图（如带「分析/趋势/占比」字样）。

**Q: 提示「表不在白名单中」？**
A: 数据源新增了表但元数据未刷新 → 数据源管理页点「刷新元数据」。

**Q: 回答与业务术语对不上？**
A: 在「业务知识」维护同义词（如「大客户」→ VIP），或「语义模型」维护表/字段别名。

## 7. MCP 接入（Claude Desktop）

```json
{
  "mcpServers": {
    "datarobort": { "url": "http://localhost:8080/sse" }
  }
}
```

Claude 可通过 MCP 调用 DataRobort 的智能体能力（`chat` / `recall` 等工具）。
